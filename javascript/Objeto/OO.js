// conceitos, CÓDIGOS NÃO EXECUTAVEL

// paradgima procedural tem como foco é baseado no procedimento em trechos de cógidos que reutilizamos em varias outras partes, com as funções
processamento(valor1, valor2, valor3)

// Orientado a Objetos, OO

// agora temos dados que contem funções
// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        //....
    }
}

objeto.processamento() // acessamos o processamento através do objeto, através do dado

// principios importantes
// 1. abstração, abstrair objetos do mundo real para o código, para oq vamos utilizar
// 2. encapsulamento, manter os detalhes de implementação interna escondidos e abstraidos do usuario, deixar a interface de comunicação simples
// 3. herança(prototype), recebemos atributos e caracteristicas dos objetos pai 
// 4. poliformismo, multiplas forma, com uma variavel mais geral, usamos elas em diferentes eventos e dados