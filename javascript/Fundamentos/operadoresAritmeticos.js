console.log('operadores que operam com dois operandos, ou seja com dois elementos ');

const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d; // a mas ta usando 4 elementos nessa operação, não primeiro soma a + b e depois esse resultado + c e esse resultado + d;

console.log(' aqui é a matematica normal');

const subtracao = d - b 
const multiplicacao = a * b
const divisao = d / a 
const modulo = a % 2

console.log( soma, subtracao, divisao, multiplicacao, modulo);