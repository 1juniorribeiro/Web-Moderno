console.log('objetos em js é uma coleção de atributos chave valor, onde colocamos um nome(chave) e um valor(atributo)')

const prod1 = {} // notação literal de um objeto
prod1.nome = 'Celular ultra mega' // aqui a chave nome foi criada dinamicamente e atribuido um valor
prod1.preco = 4888;

console.log(prod1);

const prod2 = {
    nome: 'camisa polo',
    preco: 79.90,
}

console.log(prod2, 'um conjunto de pares chave identificador');
